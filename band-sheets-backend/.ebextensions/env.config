option_settings:
  aws:elasticbeanstalk:application:environment:
    NODE_ENV: production
    # You'll need to set these environment variables in the AWS Elastic Beanstalk console
    # MONGO_URI: mongodb+srv://username:password@cluster.mongodb.net/band_sheets
    # JWT_SECRET: your_jwt_secret
    # JWT_EXPIRE: 30d
